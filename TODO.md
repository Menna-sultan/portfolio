# TODO - Tech Stack refactor

- [x] Update `Technical.vue` section title from **Tools & Technologies** to **Tech Stack**.
- [ ] Replace the two separate technology grids with a **single** responsive grid.

- [ ] Create a single `techStack` array and render the cards using `v-for`.
- [ ] Ensure all Font Awesome icons are monochrome using a single `text-slate-300` (or similar) class.
- [ ] Add missing technologies from the required list if they aren’t already present.
- [ ] Verify card sizing/spacing/hover/shadows/rounded corners remain unchanged.
- [ ] Run the dev server and visually confirm only this section changed.

